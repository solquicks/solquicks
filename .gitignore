.DS_Store
.claude/

# wrangler local state
worker/.wrangler/
# Dependencies are installed, never committed. program/scripts/node_modules was
# tracked — 16,690 files — which kept them out of npm audit's normal workflow and
# put a third-party tree in every clone of a public repo.
node_modules/
worker/node_modules/
program/target/
program/.anchor/
program/test-ledger/
program/migration/uploader.json
program/migration/prepared/
program/migration/node_modules/
program/migration/upload-state.json

# generated at cutover from live D1 data, never committed
program/migration/settle.sql

# written by a failed browser test run
test/browser/failure.png

# the collectible's setup. authority.json signs the collection and the candy
# machine into existence — it holds rent, never revenue, and never leaves here.
collectible/node_modules/
collectible/authority.json
collectible/art/
collectible/prepared/
