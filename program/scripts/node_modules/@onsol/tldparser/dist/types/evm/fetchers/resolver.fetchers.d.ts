import { Provider } from 'ethers';
import { Address } from '../types/Address';
/**
 * Get a text record from the resolver contract
 *
 * @param provider Ethers provider
 * @param resolverAddress Resolver contract address
 * @param namehash The namehash of the domain
 * @param key The text record key (e.g., "com.twitter", "avatar")
 * @returns The text record value or null
 */
export declare function getTextRecord(provider: Provider, resolverAddress: Address, namehash: string, key: string): Promise<string | null>;
/**
 * Get reverse name from the resolver contract
 *
 * @param provider Ethers provider
 * @param resolverAddress Resolver contract address
 * @param reverseNode The reverse node hash
 * @returns The resolved name or null
 */
export declare function getReverseName(provider: Provider, resolverAddress: Address, reverseNode: string): Promise<string | null>;
/**
 * Get the resolver address for a name from the registry
 *
 * @param provider Ethers provider
 * @param registryAddress Registry contract address
 * @param namehash The namehash of the domain
 * @returns The resolver address or null
 */
export declare function getResolverForName(provider: Provider, registryAddress: Address, namehash: string): Promise<Address | null>;
export declare const resolverFetchers: {
    getTextRecord: typeof getTextRecord;
    getReverseName: typeof getReverseName;
    getResolverForName: typeof getResolverForName;
};
//# sourceMappingURL=resolver.fetchers.d.ts.map