import { Provider } from 'ethers';
import { Address } from '../types/Address';
import { EvmChainData } from '../types/EvmChainData';
export type TLD = {
    controller: Address;
    registrar: Address;
    tld: string;
    name: string;
    symbol: string;
    locked: boolean;
    node: string;
    label: string;
};
export type PriceSchema = {
    for1: number;
    for2: number;
    for3: number;
    for4: number;
    for5plus: number;
};
export type SplitSchema = {
    percentage: number;
    recipient: Address;
};
declare function getRegistryAddress(params: {
    config: EvmChainData;
    provider: Provider;
}): Promise<Address>;
declare function getTldData(params: {
    config: EvmChainData;
    provider: Provider;
    tldLabel: string;
}): Promise<TLD>;
declare function getTlds(params: {
    config: EvmChainData;
    provider: Provider;
}): Promise<TLD[]>;
export declare const rootFetchers: {
    getRegistryAddress: typeof getRegistryAddress;
    getTlds: typeof getTlds;
    getTldData: typeof getTldData;
};
export {};
//# sourceMappingURL=root.fetchers.d.ts.map