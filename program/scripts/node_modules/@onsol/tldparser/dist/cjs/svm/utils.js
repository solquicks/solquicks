"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getMainDomainChecked = exports.getMultipleMainDomainsChecked = exports.getOwnedDomains = exports.getParsedAllDomainsNftAccountsByOwner = exports.findMetadataAddress = void 0;
exports.getNameAccountKeyWithBump = getNameAccountKeyWithBump;
exports.getNameOwner = getNameOwner;
exports.getHashedName = getHashedName;
exports.getOriginNameAccountKey = getOriginNameAccountKey;
exports.findOwnedNameAccountsForUser = findOwnedNameAccountsForUser;
exports.findMainDomain = findMainDomain;
exports.getAllTld = getAllTld;
exports.getTldFromTldHouseAccountInfo = getTldFromTldHouseAccountInfo;
exports.getParentAccountFromTldHouseAccountInfo = getParentAccountFromTldHouseAccountInfo;
exports.findAllDomainsForTld = findAllDomainsForTld;
exports.getMintOwner = getMintOwner;
exports.findNftRecord = findNftRecord;
exports.findTldHouse = findTldHouse;
exports.findNameHouse = findNameHouse;
exports.performReverseLookupBatched = performReverseLookupBatched;
exports.delay = delay;
exports.getUserNfts = getUserNfts;
exports.splitDomainTld = splitDomainTld;
exports.findMintAddress = findMintAddress;
exports.findRenewableMintAddress = findRenewableMintAddress;
exports.getDomainMintAccountKey = getDomainMintAccountKey;
exports.getMintAccountFromDomainTld = getMintAccountFromDomainTld;
const web3_js_1 = require("@solana/web3.js");
const bn_js_1 = require("bn.js");
const sha2_1 = require("@ethersproject/sha2");
const constants_1 = require("./constants");
const name_record_header_1 = require("./state/name-record-header");
const tag_1 = require("./types/tag");
const nft_record_1 = require("./state/nft-record");
const main_domain_1 = require("./state/main-domain");
/**
 * retrieves raw name account
 *
 * @param hashedName hashed name of the name account
 * @param nameClass defaults to pubkey::default()
 * @param parentName defaults to pubkey::default()
 */
function getNameAccountKeyWithBump(hashedName, nameClass, parentName) {
    const seeds = [
        hashedName,
        nameClass ? nameClass.toBuffer() : Buffer.alloc(32),
        parentName ? parentName.toBuffer() : Buffer.alloc(32),
    ];
    return web3_js_1.PublicKey.findProgramAddressSync(seeds, constants_1.ANS_PROGRAM_ID);
}
/**
 * retrieves owner of the name account
 *
 * @param connection sol connection
 * @param nameAccountKey nameAccount to get owner of.
 */
async function getNameOwner(connection, nameAccountKey, tldHouse) {
    const nameAccount = await name_record_header_1.NameRecordHeader.fromAccountAddress(connection, nameAccountKey);
    const owner = nameAccount.owner;
    if (!nameAccount.isValid)
        return undefined;
    if (!tldHouse)
        return owner;
    const [nameHouse] = findNameHouse(tldHouse);
    const [nftRecord] = findNftRecord(nameAccountKey, nameHouse);
    if (owner?.toBase58() !== nftRecord.toBase58())
        return owner;
    return await getMintOwner(connection, nftRecord);
}
/**
 * computes hashed name
 *
 * @param name any string or domain name.
 */
async function getHashedName(name) {
    const input = name_record_header_1.NameRecordHeader.HASH_PREFIX + name;
    const str = (0, sha2_1.sha256)(Buffer.from(input, 'utf8')).slice(2);
    return Buffer.from(str, 'hex');
}
/**
 * A constant in tld house.
 *
 * get origin name account should always equal to 3mX9b4AZaQehNoQGfckVcmgmA6bkBoFcbLj9RMmMyNcU
 *
 * @param originTld
 */
async function getOriginNameAccountKey(originTld = constants_1.ORIGIN_TLD) {
    const hashed_name = await getHashedName(originTld);
    const [nameAccountKey] = getNameAccountKeyWithBump(hashed_name, undefined, undefined);
    return nameAccountKey;
}
/**
 * finds list of all name accounts for a particular user.
 *
 * @param connection sol connection
 * @param userAccount user's public key
 * @param parentAccount nameAccount's parentName
 */
async function findOwnedNameAccountsForUser(connection, userAccount, parentAccount) {
    const filters = [
        {
            memcmp: {
                offset: 40,
                bytes: userAccount.toBase58(),
                encoding: 'base58',
            },
        },
    ];
    if (parentAccount) {
        filters.push({
            memcmp: {
                offset: 8,
                bytes: parentAccount.toBase58(),
                encoding: 'base58',
            },
        });
    }
    const accounts = await connection.getProgramAccounts(constants_1.ANS_PROGRAM_ID, {
        filters: filters,
        dataSlice: { offset: 0, length: 200 }
    });
    return accounts.map(a => {
        return {
            pubkey: a.pubkey,
            data: name_record_header_1.NameRecordHeader.fromAccountInfo(a.account),
        };
    });
}
function findMainDomain(user) {
    return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from(constants_1.MAIN_DOMAIN_PREFIX), user.toBuffer()], constants_1.TLD_HOUSE_PROGRAM_ID);
}
/**
 * finds list of all tld house accounts live.
 *
 * @param connection sol connection
 */
async function getAllTld(connection) {
    const filters = [
        {
            memcmp: {
                offset: 0,
                bytes: 'iQgos3SdaVE',
            },
        },
    ];
    const accounts = await connection.getProgramAccounts(constants_1.TLD_HOUSE_PROGRAM_ID, {
        filters: filters,
    });
    const tldsAndParentAccounts = [];
    accounts.map(({ account }) => {
        const parentAccount = getParentAccountFromTldHouseAccountInfo(account);
        const tld = getTldFromTldHouseAccountInfo(account);
        tldsAndParentAccounts.push({ tld, parentAccount });
    });
    return tldsAndParentAccounts;
}
function getTldFromTldHouseAccountInfo(tldHouseData) {
    const tldStart = 8 + 32 + 32 + 32;
    const tldBuffer = tldHouseData?.data?.subarray(tldStart);
    const nameLength = new bn_js_1.BN(tldBuffer?.subarray(0, 4), 'le').toNumber();
    return tldBuffer
        .subarray(4, 4 + nameLength)
        .toString()
        .replace(/\0.*$/g, '');
}
function getParentAccountFromTldHouseAccountInfo(tldHouseData) {
    const parentAccountStart = 8 + 32 + 32;
    const parentAccountBuffer = tldHouseData?.data?.subarray(parentAccountStart, parentAccountStart + 32);
    return new web3_js_1.PublicKey(parentAccountBuffer);
}
/**
 * finds list of all domains in parent account from tld.
 *
 * @param connection sol connection
 * @param parentAccount nameAccount's parentName
 */
async function findAllDomainsForTld(connection, parentAccount) {
    const filters = [
        {
            memcmp: {
                offset: 8,
                bytes: parentAccount.toBase58(),
                encoding: 'base58',
            },
        },
    ];
    const accounts = await connection.getProgramAccounts(constants_1.ANS_PROGRAM_ID, {
        filters: filters,
        dataSlice: { offset: 8, length: 40 },
    });
    return accounts.map((a) => a.pubkey);
}
async function getMintOwner(connection, nftRecord) {
    try {
        const nftRecordData = await nft_record_1.NftRecord.fromAccountAddress(connection, nftRecord);
        if (nftRecordData.tag !== tag_1.Tag.ActiveRecord)
            return;
        const largestAccounts = await connection.getTokenLargestAccounts(nftRecordData.nftMintAccount);
        const largestAccountInfo = await connection.getParsedAccountInfo(largestAccounts.value[0].address);
        if (!largestAccountInfo.value.data)
            return;
        // @ts-ignore
        return new web3_js_1.PublicKey(largestAccountInfo.value.data.parsed.info.owner);
    }
    catch {
        return undefined;
    }
}
function findNftRecord(nameAccount, nameHouseAccount) {
    return web3_js_1.PublicKey.findProgramAddressSync([
        Buffer.from(constants_1.NFT_RECORD_PREFIX),
        nameHouseAccount.toBuffer(),
        nameAccount.toBuffer(),
    ], constants_1.NAME_HOUSE_PROGRAM_ID);
}
function findTldHouse(tldString) {
    tldString = tldString.toLowerCase();
    return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from(constants_1.TLD_HOUSE_PREFIX), Buffer.from(tldString)], constants_1.TLD_HOUSE_PROGRAM_ID);
}
function findNameHouse(tldHouse) {
    return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from(constants_1.NAME_HOUSE_PREFIX), tldHouse.toBuffer()], constants_1.NAME_HOUSE_PROGRAM_ID);
}
const findMetadataAddress = (mint) => {
    return web3_js_1.PublicKey.findProgramAddressSync([
        Buffer.from('metadata'),
        constants_1.TOKEN_METADATA_PROGRAM_ID.toBuffer(),
        mint.toBuffer(),
    ], constants_1.TOKEN_METADATA_PROGRAM_ID)[0];
};
exports.findMetadataAddress = findMetadataAddress;
async function performReverseLookupBatched(connection, nameAccounts, tldHouse) {
    const promises = nameAccounts.map(async (nameAccount) => {
        const reverseLookupHashedName = await getHashedName(nameAccount.toBase58());
        const [reverseLookUpAccount] = getNameAccountKeyWithBump(reverseLookupHashedName, tldHouse, undefined);
        return reverseLookUpAccount;
    });
    const reverseLookUpAccounts = await Promise.all(promises);
    const reverseLookupAccountInfos = await connection.getMultipleAccountsInfo(reverseLookUpAccounts);
    return reverseLookupAccountInfos.map(reverseLookupAccountInfo => {
        const domain = reverseLookupAccountInfo?.data
            .subarray(name_record_header_1.NameRecordHeader.byteSize, reverseLookupAccountInfo?.data.length)
            .toString();
        return domain;
    });
}
function delay(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
}
async function getUserNfts(owner, connection) {
    const { value: splAccounts } = await connection.getParsedTokenAccountsByOwner(owner, {
        programId: constants_1.SPL_TOKEN_PROGRAM_ID,
    });
    const nftAccounts = splAccounts
        .filter(t => {
        const amount = t.account?.data?.parsed?.info?.tokenAmount?.uiAmount;
        const decimals = t.account?.data?.parsed?.info?.tokenAmount?.decimals;
        return decimals === 0 && amount === 1;
    })
        .map(t => {
        const address = t.account?.data?.parsed?.info?.mint;
        return address;
    });
    return nftAccounts;
}
const getParsedAllDomainsNftAccountsByOwner = async (owner, connection, expectedCreator) => {
    const nftAccounts = await getUserNfts(owner, connection);
    const ownerDomains = await (0, exports.getOwnedDomains)(nftAccounts, connection, expectedCreator);
    return ownerDomains;
};
exports.getParsedAllDomainsNftAccountsByOwner = getParsedAllDomainsNftAccountsByOwner;
const getOwnedDomains = async (nftAddresses, connection, expectedCreator) => {
    const ownedDomains = [];
    const verifiedCreatorByteOffset = 326;
    const verifiedCreatorVerfiiedByteOffset = 326;
    if (nftAddresses.length > 100) {
        while (nftAddresses.length > 0) {
            let nftMetadataKeys = nftAddresses
                .splice(0, 100)
                .map((mint) => (0, exports.findMetadataAddress)(new web3_js_1.PublicKey(mint)));
            const nftsMetadata = await connection.getMultipleAccountsInfo(nftMetadataKeys);
            for (const nftMetadata of nftsMetadata) {
                if (nftMetadata) {
                    const verifiedCreatorAddress = new web3_js_1.PublicKey(nftMetadata.data.subarray(verifiedCreatorByteOffset, verifiedCreatorByteOffset + 32));
                    const isVerified = Boolean(nftMetadata.data.subarray(verifiedCreatorVerfiiedByteOffset, verifiedCreatorVerfiiedByteOffset + 1));
                    if (isVerified &&
                        verifiedCreatorAddress.toString() ===
                            expectedCreator.toString()) {
                        const domainName = nftMetadata.data
                            .subarray(66, 101)
                            .toString()
                            .replace(/\u0000/g, '');
                        ownedDomains.push(domainName);
                    }
                }
            }
        }
    }
    else {
        let nftMetadataKeys = nftAddresses.map((mint) => (0, exports.findMetadataAddress)(new web3_js_1.PublicKey(mint)));
        const nftsMetadata = await connection.getMultipleAccountsInfo(nftMetadataKeys);
        for (const nftMetadata of nftsMetadata) {
            if (nftMetadata) {
                const verifiedCreatorAddress = new web3_js_1.PublicKey(nftMetadata.data.subarray(verifiedCreatorByteOffset, verifiedCreatorByteOffset + 32));
                const isVerified = Boolean(nftMetadata.data.subarray(verifiedCreatorVerfiiedByteOffset, verifiedCreatorVerfiiedByteOffset + 1));
                if (isVerified &&
                    verifiedCreatorAddress.toString() ===
                        expectedCreator.toString()) {
                    const domainName = nftMetadata.data
                        .subarray(66, 101)
                        .toString()
                        .replace(/\u0000/g, '');
                    ownedDomains.push(domainName);
                }
            }
        }
    }
    return ownedDomains;
};
exports.getOwnedDomains = getOwnedDomains;
function splitDomainTld(domain) {
    const parts = domain.split('.');
    let tld = '', domainName = '', subdomain = '';
    if (parts.length === 1) {
        domainName = parts[0];
    }
    else {
        tld = '.' + parts[parts.length - 1];
        domainName = parts[parts.length - 2];
        subdomain = parts.slice(0, parts.length - 2).join('.');
    }
    return [tld, domainName, subdomain];
}
function findMintAddress(nameAccount, nameHouseAccount) {
    return web3_js_1.PublicKey.findProgramAddressSync([
        Buffer.from(constants_1.NAME_HOUSE_PREFIX),
        nameHouseAccount.toBuffer(),
        nameAccount.toBuffer(),
    ], constants_1.NAME_HOUSE_PROGRAM_ID);
}
function findRenewableMintAddress(nameAccount, nameHouseAccount, expiresAtBuffer) {
    return web3_js_1.PublicKey.findProgramAddressSync([
        Buffer.from(constants_1.NAME_HOUSE_PREFIX),
        nameHouseAccount.toBuffer(),
        nameAccount.toBuffer(),
        expiresAtBuffer,
    ], constants_1.NAME_HOUSE_PROGRAM_ID);
}
/**
 * retrieves owner of the name account
 *
 * @param connection sol connection
 * @param nameAccountKey nameAccount to get owner of.
 */
async function getDomainMintAccountKey(connection, nameAccountKey, tldHouse) {
    const nameAccount = await name_record_header_1.NameRecordHeader.fromAccountAddress(connection, nameAccountKey);
    const expiryDate = nameAccount.expiresAt;
    const secondSinceEpoch = new Date(0);
    let mintAccount;
    const [nameHouse] = findNameHouse(tldHouse);
    if (expiryDate === secondSinceEpoch) {
        [mintAccount] = findMintAddress(nameAccountKey, nameHouse);
    }
    else {
        [mintAccount] = findRenewableMintAddress(nameAccountKey, nameHouse, dateToU64Buffer(expiryDate));
    }
    return mintAccount;
}
async function getMintAccountFromDomainTld(connection, domainTld) {
    const domainTldSplit = domainTld.split('.');
    const domain = domainTldSplit[0];
    const tldName = '.' + domainTldSplit[1];
    const nameOriginTldKey = await getOriginNameAccountKey();
    const parentHashedName = await getHashedName(tldName);
    const [parentAccountKey] = getNameAccountKeyWithBump(parentHashedName, undefined, nameOriginTldKey);
    const domainHashedName = await getHashedName(domain);
    const [domainAccountKey] = getNameAccountKeyWithBump(domainHashedName, undefined, parentAccountKey);
    const [tldHouse] = findTldHouse(tldName);
    return await getDomainMintAccountKey(connection, domainAccountKey, tldHouse);
}
function dateToU64Buffer(expiryDate) {
    const secondsSinceEpoch = Math.floor(expiryDate.getTime() / 1000);
    const buffer = Buffer.alloc(8);
    buffer.writeBigUInt64BE(BigInt(secondsSinceEpoch));
    return buffer;
}
const getMultipleMainDomainsChecked = async (connection, pubkeys) => {
    const mainDomainKeys = pubkeys.map(pubkey => findMainDomain(new web3_js_1.PublicKey(pubkey))[0]);
    // fetch main domain accounts
    const mainDomainAccounts = await connection.getMultipleAccountsInfo(mainDomainKeys);
    const mainDomainWithNameAccounts = pubkeys.map((pubkey, index) => {
        const mainDomainAccount = mainDomainAccounts[index];
        if (!!mainDomainAccount?.data) {
            const mainDomainData = main_domain_1.MainDomain.fromAccountInfo(mainDomainAccount)[0];
            const nameAccount = mainDomainData.nameAccount;
            return {
                pubkey: pubkey.toString(),
                nameAccount,
                mainDomain: mainDomainData.domain + mainDomainData.tld,
            };
        }
        return {
            pubkey: pubkey.toString(),
            nameAccount: undefined,
            mainDomain: undefined,
        };
    });
    // fetch name accounts
    const nameAccounts = mainDomainWithNameAccounts.map(item => item.nameAccount);
    const filteredNameAccountsWithIndex = nameAccounts
        .map((pk, idx) => (pk ? { pk, idx } : undefined))
        .filter((x) => !!x);
    const filteredNameAccounts = filteredNameAccountsWithIndex.map(x => x.pk);
    const nameAccountsInfo = await connection.getMultipleAccountsInfo(filteredNameAccounts);
    // map infoByOriginalIdx
    const infoByOriginalIdx = new Array(nameAccounts.length).fill(null);
    filteredNameAccountsWithIndex.forEach((entry, i) => {
        infoByOriginalIdx[entry.idx] = nameAccountsInfo[i];
    });
    const result = await Promise.all(mainDomainWithNameAccounts.map(async (item, index) => {
        const nameAccount = item.nameAccount;
        const info = infoByOriginalIdx[index];
        if (!nameAccount || !info) {
            return { ...item };
        }
        const nameAccountData = name_record_header_1.NameRecordHeader.fromAccountInfo(info);
        const tld = item.mainDomain?.split('.')[1];
        const [tldHouseKey] = findTldHouse(tld);
        const [nameHouseKey] = findNameHouse(tldHouseKey);
        const [nftRecordKey] = findNftRecord(nameAccount, nameHouseKey);
        // check if the main domain owner is the nftrecord onchain
        const isNftRecordOwner = nameAccountData.owner?.toString() === nftRecordKey.toString();
        if (isNftRecordOwner) {
            // check if the nft owner is the main domain owner
            const mintOwner = await getMintOwner(connection, nftRecordKey);
            if (mintOwner?.toString() != pubkeys[index]) {
                return {
                    pubkey: pubkeys[index].toString(),
                    nameAccount,
                    mainDomain: undefined,
                };
            }
            return { ...item };
        }
        // check if the name record owner is the main domain owner
        if (nameAccountData.owner?.toString() != pubkeys[index]) {
            return {
                pubkey: pubkeys[index].toString(),
                nameAccount,
                mainDomain: undefined,
            };
        }
        // check if the name record expires at is valid
        const expiresAtIsValid = nameAccountData.expiresAt.getTime() != 0;
        if (expiresAtIsValid &&
            nameAccountData.expiresAt.getTime() < Date.now()) {
            return {
                pubkey: pubkeys[index].toString(),
                nameAccount,
                mainDomain: undefined,
            };
        }
        return { ...item };
    }));
    return result;
};
exports.getMultipleMainDomainsChecked = getMultipleMainDomainsChecked;
const getMainDomainChecked = async (connection, pubkey) => {
    const mainDomainKey = findMainDomain(new web3_js_1.PublicKey(pubkey))[0];
    const mainDomainAccount = await connection.getAccountInfo(mainDomainKey);
    if (!mainDomainAccount?.data) {
        return { pubkey, nameAccount: undefined, mainDomain: undefined };
    }
    const mainDomainData = main_domain_1.MainDomain.fromAccountInfo(mainDomainAccount)[0];
    const nameAccount = mainDomainData.nameAccount;
    const nameAccountInfo = await connection.getAccountInfo(nameAccount);
    if (!nameAccountInfo) {
        return { pubkey, nameAccount, mainDomain: undefined };
    }
    const nameAccountData = name_record_header_1.NameRecordHeader.fromAccountInfo(nameAccountInfo);
    const tld = mainDomainData.tld;
    const [tldHouseKey] = findTldHouse(tld);
    const [nameHouseKey] = findNameHouse(tldHouseKey);
    const [nftRecordKey] = findNftRecord(nameAccount, nameHouseKey);
    // NFT owner check
    if (nameAccountData.owner?.toString() === nftRecordKey.toString()) {
        const mintOwner = await getMintOwner(connection, nftRecordKey);
        if (mintOwner?.toString() !== pubkey) {
            return { pubkey, nameAccount, mainDomain: undefined };
        }
        return {
            pubkey,
            nameAccount,
            mainDomain: mainDomainData.domain + mainDomainData.tld,
        };
    }
    // Name record owner check
    if (nameAccountData.owner?.toString() !== pubkey) {
        return { pubkey, nameAccount, mainDomain: undefined };
    }
    // Expiry check
    const expiresAtIsValid = nameAccountData.expiresAt.getTime() !== 0;
    if (expiresAtIsValid && nameAccountData.expiresAt.getTime() < Date.now()) {
        return { pubkey, nameAccount, mainDomain: undefined };
    }
    return {
        pubkey,
        nameAccount,
        mainDomain: mainDomainData.domain + mainDomainData.tld,
    };
};
exports.getMainDomainChecked = getMainDomainChecked;
//# sourceMappingURL=utils.js.map